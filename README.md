# 🚀 Інструкція по налаштуванню

## 📋 Що потрібно зробити:

### 1. Встановити залежності
```bash
pip install -r requirements.txt
```

### 2. Створити бота в Telegram

1. Відкрий [@BotFather](https://t.me/BotFather)
2. Надішли команду `/newbot`
3. Вибери ім'я та username для бота
4. Отримаєш `BOT_TOKEN` - збережи його

### 3. Отримати API credentials

1. Перейди на https://my.telegram.org/apps
2. Авторизуйся і створи додаток
3. Отримаєш `API_ID` та `API_HASH`

### 4. Отримати свій User ID

1. Відкрий [@userinfobot](https://t.me/userinfobot)
2. Надішли `/start`
3. Отримаєш свій `User ID`

### 5. Отримати Gemini API Key

1. Перейди на https://aistudio.google.com/app/apikey
2. Створи новий API ключ
3. Збережи `GEMINI_API_KEY`

### 6. Налаштувати .env файл

Створи файл `.env` в корені проекту:

```env
API_ID=your_api_id
API_HASH=your_api_hash
GEMINI_API_KEY=your_gemini_api_key
USER_INFO_CHAT_ID=your_user_id
BOT_TOKEN=your_bot_token_from_botfather
YOUR_USER_ID=your_telegram_user_id
NOTIFICATION_BOT_URL=http://localhost:5000
```

## 🎯 Як запускати:

### Варіант 1: Два окремі термінали

**Термінал 1 - Notification Bot:**
```bash
python notification_bot.py
```

**Термінал 2 - Userbot:**
```bash
python main.py
```

### Варіант 2: Використати tmux/screen

```bash
# Запустити notification bot у фоні
tmux new -d -s notif_bot 'python notification_bot.py'

# Запустити userbot
python main.py
```

### Варіант 3: Systemd service (Linux)

Створи два service файли для автозапуску.

## 📱 Як це працює:

### Сценарій використання:

1. **23:00** - Починається режим сну
2. **02:30** - Хтось надсилає тобі повідомлення
3. **Автоматично:**
   - Userbot відповідає: "Я сплю, відповім пізніше"
   - Надсилає дані боту через HTTP
   - Бот надсилає тобі сповіщення з кнопкою
4. **08:30** - Ти прокидаєшся
5. **Відкриваєш бота** - бачиш всі повідомлення
6. **Натискаєш "✅ Прочитано"** - повідомлення позначається

## 🔧 Команди userbot:

- `.me` - Надіслати підготовлений текст
- `.save_chat` - Зберегти останні 100 повідомлень
- `.ai [запит]` - Запитати у AI (коротка відповідь без markdown)
- `.tn [мова] [текст]` - Перекласти текст на вказану мову
- `.tn` (як reply) - Перекласти повідомлення на українську
- `.find_user [username]` - Знайти username в базах даних
- `.user` - Отримати інформацію про користувача

## 🛡️ Безпека:

- ✅ Автовідповідач працює тільки з приватними юзерами
- ✅ Не відповідає в групи/канали
- ✅ Працює тільки з 23:00 до 8:00
- ✅ Не спамить (макс 1 відповідь на 8 годин)
- ✅ HTTP API працює локально (localhost)

## ⚠️ Важливо:

1. **Не публікуй `.env` файл!** Він містить конфіденційні дані
2. **Notification bot** має бути запущений **ДО** userbot
3. Якщо бот не встигає обробити запит, userbot продовжить працювати
4. Логи помилок будуть в консолі

## 🐛 Troubleshooting:

**Проблема:** Бот не надсилає сповіщення
- Перевір чи запущений `notification_bot.py`
- Перевір `NOTIFICATION_BOT_URL` в `.env`
- Перевір логи в консолі

**Проблема:** Maigret не працює
```bash
pip install maigret
```

**Проблема:** Gemini не відповідає
- Перевір чи правильний `GEMINI_API_KEY`
- Перевір квоту API на https://aistudio.google.com

## 📞 Підтримка:

Якщо виникли питання - створи issue на GitHub!